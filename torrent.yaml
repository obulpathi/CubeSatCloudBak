# Configuration

commands:
    - {operation: SENSE, filename: image.jpg, lat: lat, lon: lon}
    - {operation: STORE, filename: image.jpg}
    - {operation: DOWNLINK, filename: result.jpg}

server:
    address: localhost
    port: 4000
    homedir: ~/cloud/data/server/

groundstation:
    address: localhost
    port: 4004

master:
    address: localhost
    port: 8000
    ssport: 8004
    homedir: ~/cloud/data/master/
    
worker:
    address: localhost
    port: 8008
    homedir: ~/cloud/data/
